# Gmail Multi-Account OAuth Configuration
# Manages OAuth2 credentials for different Gmail accounts

gmail_accounts:
  # Configure each Gmail account used for bank statement sync
  yashwanth.sunny8047@gmail.com:
    credentials_file: credentials_yashwanth.json
    token_file: token_yashwanth.json

  # Legacy account for reference (not used currently)
  saiyashwanthmamilla@gmail.com:
    credentials_file: credentials.json  # Use same OAuth app but separate tokens
    token_file: token_work.json
